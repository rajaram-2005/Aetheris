# Phase 3.1 — Self-Reflection: evaluate failure, identify step, store feedback
class SelfReflection:
    def reflect(self, task, solution, error):
        return {"reason": str(error), "step_failed": "execution", "improvement": "add validation"}
